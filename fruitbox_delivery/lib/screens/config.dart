import 'package:flutter/material.dart';

class AppImages {
  static const welcomeImage = 'assets/images/deliveryimg.png';
// Add more image paths as needed
}

height(context)=> MediaQuery.of(context).size.height;